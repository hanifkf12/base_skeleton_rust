APP_HOST=0.0.0.0
APP_PORT=3000
DATABASE_URL=postgres://postgres:postgres@localhost:5432/base_skeleton
# Optional privileged migration connection. Falls back to DATABASE_URL.
MIGRATION_DATABASE_URL=
DATABASE_MAX_CONNECTIONS=10
REDIS_URL=redis://localhost:6379
REDIS_CONNECT_TIMEOUT_SECONDS=3
USER_CACHE_TTL_SECONDS=300
REQUEST_TIMEOUT_SECONDS=10
MAX_REQUEST_BODY_BYTES=65536
# Required only by the `http` and `all` commands. The issuer must expose OIDC discovery and JWKS.
OIDC_ISSUER_URL=http://localhost:8081/realms/demo
OIDC_AUDIENCE=base-skeleton-api
# Comma-delimited asymmetric JWT algorithms. Symmetric HMAC algorithms are rejected.
OIDC_ALLOWED_ALGORITHMS=RS256
# Local development uses HTTP Keycloak. Set to false in production and use HTTPS issuer/JWKS URLs.
OIDC_ALLOW_INSECURE_HTTP=true
OIDC_HTTP_TIMEOUT_SECONDS=5
OIDC_CLOCK_SKEW_SECONDS=30
OIDC_JWKS_REFRESH_INTERVAL_SECONDS=60
# Cached signing keys older than this must be refreshed and fail closed on provider outage.
OIDC_JWKS_MAX_AGE_SECONDS=300
# Access tokens must include iat and may not span longer than this.
OIDC_MAX_TOKEN_LIFETIME_SECONDS=3600
# Token bucket applied only to /api/* (sustained requests/minute plus burst capacity).
RATE_LIMIT_REQUESTS_PER_MINUTE=120
RATE_LIMIT_BURST=30
# Comma-separated proxy CIDRs allowed to supply X-Forwarded-For. Empty trusts no proxy.
TRUSTED_PROXY_CIDRS=
# Enables GET /metrics. Send this value as a Bearer token; keep it in secret management.
METRICS_PROMETHEUS_BEARER_TOKEN=
JOB_POLL_INTERVAL_MILLISECONDS=1000
JOB_LEASE_TIMEOUT_SECONDS=300
JOB_RETRY_BASE_SECONDS=5
JOB_RETRY_MAX_SECONDS=300
JOB_MAX_ATTEMPTS=5
# Optional. A unique ID is generated when omitted.
JOB_WORKER_ID=
JOB_COMPLETED_RETENTION_SECONDS=86400
JOB_DEAD_RETENTION_SECONDS=2592000
JOB_CLEANUP_INTERVAL_SECONDS=3600
RUST_LOG=base_skeleton_rust=info,tower_http=info
# SigNoz OTLP/HTTP endpoint. For self-hosted SigNoz use http://localhost:4318.
# For SigNoz Cloud use the regional ingest endpoint, for example https://ingest.us.signoz.cloud:443.
OTEL_EXPORTER_OTLP_ENDPOINT=
# SigNoz Cloud only: signoz-ingestion-key=your-ingestion-key
OTEL_EXPORTER_OTLP_HEADERS=
OTEL_SERVICE_NAME=base-skeleton-rust
